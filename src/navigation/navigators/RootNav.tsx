import {StyleSheet, Text, View} from 'react-native';

const RootNav = () => {
  return (
    <View>
      <Text>RootNav</Text>
    </View>
  );
};

export default RootNav;

const styles = StyleSheet.create({});
